# 🎲 Telegram Betting Number Bot

A Telegram bot that allows users to pick unique numbers for a betting-style game.
Once a number is picked, it cannot be selected by another user.

Built with **Python** and **python-telegram-bot**, designed to run on **Render**, **Railway**, or **Fly.io**.

---

## ✨ Features

- `/start` – Welcome message & instructions
- `/numbers` – View available (unpicked) numbers
- `/pick <number>` – Pick and reserve a number
- Prevents duplicate number selection
- Admin-only command to view all picked numbers
- Simple database using SQLite

---

## 🧱 Project Structure

